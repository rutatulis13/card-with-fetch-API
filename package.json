{
  "name": "frontend",
  "version": "1.0.0",
  "description": "nauja aplikacija",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dead-server --port=5000 --host=localhost"
  },
  "keywords": [
    "node"
  ],
  "author": "Ruta Zuke",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
